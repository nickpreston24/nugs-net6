// using Microsoft.AspNetCore.Razor.TagHelpers.TagHelper;

namespace nugsnet6.Experimental;

public static class AspNetPartialExtensions
{
    public static object CreateDropdown(this object blah)
    {
        // TODO: try a safe html string from Asp's libary.

        // var x = @Html.DropDownList("Role");

        return default;
    }
}